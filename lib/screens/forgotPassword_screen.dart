import 'package:flutter/material.dart';

class ForPasScreen extends StatelessWidget {
  const ForPasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Forgot Password Screen'),
      ),
    );
  }
}